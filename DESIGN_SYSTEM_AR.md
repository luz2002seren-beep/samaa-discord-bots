# نظام التصميم والتنفيذ — سماع

## برومبت المنتج

> أنت مهندس Discord ومهندس موثوقية للصوت بخبرة طويلة. صمّم بوت موسيقى عربيًا سريعًا وهادئًا يخدم مجتمعات كبيرة. اجعل كل تفاعل واضحًا ومختصرًا، وافصل واجهة الأوامر عن طبقة الصوت. لا تستخدم تنزيل الصوت داخل البوت: فوّض الاستخراج والبث إلى NodeLink v3، واعرض للمستخدم نتيجة مفهومة عند فشل مصدر. ادعم الاستعلامات وروابط YouTube وروابط Spotify، بما فيها قوائم Spotify، بالمصادر المدمجة في NodeLink. احمِ الرومات بقناة أوامر مخصصة، واحفظ تفضيلات السيرفر وقوائم المستخدمين بصورة آمنة وقابلة للاستبدال بقاعدة بيانات لاحقًا.

## مبادئ النظام

- محرك الصوت: NodeLink v3 + Shoukaku؛ يضيف NodeLink مصادر مدمجة، clustering، ومزايا تشغيل متقدمة.
- مصادر Spotify: Spotify لا يوفر بثًا صوتيًا مباشرًا للبوت؛ يحوّل NodeLink بيانات الرابط إلى مصدر تشغيل متاح عند تزويده بمفاتيح Spotify.
- الأوامر بلا بادئة تعمل فقط في روم الأوامر المعيّن، ما عدا إعداد `ثتبيت`/`تثبيت` و`afk` للمشرف.
- كل أمر موسيقى يتأكد من وجود المستخدم في روم صوتي قبل الاتصال أو التحكم.
- التخزين الحالي JSON محلي لبدء سريع؛ بدّله بـPostgreSQL/Redis في النشر متعدد النسخ.
